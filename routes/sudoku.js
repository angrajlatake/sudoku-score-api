const express = require("express");
const router = express.Router();
const fs = require("fs");


//gets new sudoku board depending on difficulty

//gets generated sudoku board by id
router.get("/:id", (req, res) => {

})

module.exports = router;